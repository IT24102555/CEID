<!DOCTYPE html>
<html lang="en" xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin - Application</title>
    <style>
        body{background:linear-gradient(135deg,#0a0a1a,#1a1a2e);color:#e0e0e0;font-family:'Segoe UI',Tahoma,Geneva,Verdana,sans-serif;min-height:100vh}
        .sidebar{position:fixed;top:0;left:0;height:100vh;width:250px;background:linear-gradient(180deg,#0c0c1f,#1a1a3a);color:#fff;box-shadow:2px 0 20px rgba(0,0,0,.3);border-right:1px solid rgba(59,130,246,.1);} 
        .logo{font-weight:bold;background:linear-gradient(to right,#60a5fa,#3b82f6);-webkit-background-clip:text;-webkit-text-fill-color:transparent;text-decoration:none}
        .content{margin-left:250px;padding:24px}
        .card{background:rgba(15,23,42,.9);border:1px solid rgba(59,130,246,.1);border-radius:12px;padding:24px;box-shadow:0 10px 30px rgba(0,0,0,.4)}
        .title{font-size:24px;color:#e0e7ff;margin-bottom:12px}
        .input, .textarea, select{width:100%;padding:12px 14px;border-radius:8px;border:1px solid #334155;background:rgba(30,41,59,.7);color:#e2e8f0}
        .row{display:flex;gap:12px;margin-top:16px}
        .btn{background:linear-gradient(135deg,#2563eb,#1d4ed8);border:none;color:#fff;padding:12px 18px;border-radius:8px;text-decoration:none}
        .docs a{color:#60a5fa;text-decoration:none}
    </style>
</head>
<body>
    <div class="sidebar">
        <div style="padding:1.5rem;text-align:center;border-bottom:1px solid rgba(255,255,255,.1)"><a href="/admin" class="logo"><i class="fas fa-chart-line" style="margin-right:.5rem"></i>Ceylon Electric</a></div>
        <nav style="padding:1rem 0">
            <div><a href="/admin" style="display:flex;align-items:center;color:rgba(255,255,255,.8);padding:.75rem 1.5rem;text-decoration:none">Dashboard</a></div>
            <div><a href="/admin/applications" style="display:flex;align-items:center;color:#fff;background-color:rgba(59,130,246,.2);border-left:3px solid #3b82f6;padding:.75rem 1.5rem;text-decoration:none">Applications</a></div>
            <div><a href="/admin/officers" style="display:flex;align-items:center;color:rgba(255,255,255,.8);padding:.75rem 1.5rem;text-decoration:none">Tracking Officer</a></div>
        </nav>
        <div style="position:absolute;bottom:0;left:0;right:0;padding:1.5rem;border-top:1px solid rgba(255,255,255,.1)"><a href="/logout" class="btn">Logout</a></div>
    </div>
    <div class="content">
        <div class="card">
            <div class="title">Review Application</div>
            <div style="display:grid;grid-template-columns:200px 1fr;gap:8px 12px">
                <div>Name</div><div th:text="${app.fullName}"></div>
                <div>Address</div><div th:text="${app.address}"></div>
                <div>Contact</div><div th:text="${app.contactNumber}"></div>
                <div>Status</div><div th:text="${app.status}"></div>
            </div>

            <div class="docs" style="margin-top:16px">
                <div style="color:#94a3b8">Uploaded documents (if stored as files): navigate server folder uploads/applications/<span th:text="${app.id}"></span></div>
            </div>

            <form th:action="@{'/admin/applications/' + ${app.id}}" method="post" style="margin-top:16px">
                <div class="row">
                    <select name="status" class="input" style="max-width:250px">
                        <option value="PENDING">PENDING</option>
                        <option value="UNDER REVIEW">UNDER REVIEW</option>
                        <option value="APPROVED">APPROVED</option>
                        <option value="REJECTED">REJECTED</option>
                        <option value="ISSUED">ISSUED</option>
                        <option value="DELIVERED">DELIVERED</option>
                    </select>
                    <input class="input" name="remarks" placeholder="Remarks (optional)" />
                    <button type="submit" class="btn">Update Status</button>
                </div>
            </form>
        </div>
    </div>
</body>
</html>








