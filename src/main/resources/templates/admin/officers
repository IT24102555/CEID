<!DOCTYPE html>
<html lang="en" xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin - Tracking Officer</title>
    <style>
        body{background:linear-gradient(135deg,#0a0a1a,#1a1a2e);color:#e0e0e0;font-family:'Segoe UI',Tahoma,Geneva,Verdana,sans-serif;min-height:100vh}
        .sidebar{position:fixed;top:0;left:0;height:100vh;width:250px;background:linear-gradient(180deg,#0c0c1f,#1a1a3a);color:#fff;box-shadow:2px 0 20px rgba(0,0,0,.3);border-right:1px solid rgba(59,130,246,.1);} 
        .logo{font-weight:bold;background:linear-gradient(to right,#60a5fa,#3b82f6);-webkit-background-clip:text;-webkit-text-fill-color:transparent;text-decoration:none}
        .content{margin-left:250px;padding:24px}
        .card{background:rgba(15,23,42,.9);border:1px solid rgba(59,130,246,.1);border-radius:12px;padding:24px;box-shadow:0 10px 30px rgba(0,0,0,.4)}
        .row{display:flex;gap:12px;margin-top:16px}
        .btn{background:linear-gradient(135deg,#2563eb,#1d4ed8);border:none;color:#fff;padding:12px 18px;border-radius:8px;text-decoration:none}
        .input, .textarea{width:100%;padding:12px 14px;border-radius:8px;border:1px solid #334155;background:rgba(30,41,59,.7);color:#e2e8f0}
        .item{display:flex;justify-content:space-between;align-items:center;border:1px solid rgba(59,130,246,.1);border-radius:10px;padding:12px;margin-bottom:10px}
    </style>
</head>
<body>
    <div class="sidebar">
        <div style="padding:1.5rem;text-align:center;border-bottom:1px solid rgba(255,255,255,.1)"><a href="/admin" class="logo"><i class="fas fa-chart-line" style="margin-right:.5rem"></i>Ceylon Electric</a></div>
        <nav style="padding:1rem 0">
            <div><a href="/admin" style="display:flex;align-items:center;color:rgba(255,255,255,.8);padding:.75rem 1.5rem;text-decoration:none">Dashboard</a></div>
            <div><a href="/admin/applications" style="display:flex;align-items:center;color:rgba(255,255,255,.8);padding:.75rem 1.5rem;text-decoration:none">Applications</a></div>
            <div><a href="/admin/officers" style="display:flex;align-items:center;color:#fff;background-color:rgba(59,130,246,.2);border-left:3px solid #3b82f6;padding:.75rem 1.5rem;text-decoration:none">Tracking Officer</a></div>
        </nav>
        <div style="position:absolute;bottom:0;left:0;right:0;padding:1.5rem;border-top:1px solid rgba(255,255,255,.1)"><a href="/logout" class="btn">Logout</a></div>
    </div>
    <div class="content">
        <div class="card">
            <form th:action="@{/admin/officers}" method="post">
                <div class="row">
                    <input class="input" name="title" placeholder="Title" required />
                    <input class="input" name="details" placeholder="Details" required />
                    <button type="submit" class="btn">Add</button>
                </div>
            </form>
        </div>
        <div class="card" style="margin-top:16px">
            <div th:each="r : ${records}" class="item">
                <div>
                    <div><strong th:text="${r.title}">Title</strong></div>
                    <div style="color:#94a3b8;font-size:14px" th:text="${#temporals.format(r.updatedAt,'yyyy-MM-dd HH:mm')}"></div>
                </div>
                <div>
                    <form th:action="@{'/admin/officers/' + ${r.id}}" method="post" style="display:inline-flex; gap:8px">
                        <input class="input" name="title" th:value="${r.title}" style="max-width:200px" />
                        <input class="input" name="details" th:value="${r.details}" style="max-width:300px" />
                        <button class="btn" type="submit">Save</button>
                    </form>
                    <form th:action="@{'/admin/officers/' + ${r.id} + '/delete'}" method="post" style="display:inline">
                        <button class="btn" type="submit" style="background:linear-gradient(135deg,#ef4444,#dc2626)">Delete</button>
                    </form>
                </div>
            </div>
        </div>
    </div>
</body>
</html>








